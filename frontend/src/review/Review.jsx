// Reviewer console — Nutrient DWS Viewer (@nutrient-sdk/viewer).
// The reviewer sees the triggering signal only, never raw biometric data.
// Implemented in Step 9.
export default function Review() {
  return <main>Reviewer console — Step 9</main>
}
