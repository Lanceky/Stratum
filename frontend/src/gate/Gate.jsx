// Gate capture UI — Perfect Corp JS Camera Kit, `comprehensive` mode.
// Implemented in Step 2c; challenge-response colour flash added in Step 5.
export default function Gate() {
  return <main>Gate — Step 2c</main>
}
